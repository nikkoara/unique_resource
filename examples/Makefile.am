# -*- mode: makefile -*-

EXTRA_DIST = 

include $(top_srcdir)/Makefile.common

bin_PROGRAMS = wrapper usage

wrapper_SOURCES = wrapper.cc
wrapper_LDADD = $(LIBS)

usage_SOURCES = usage.cc
usage_LDADD = $(LIBS)
